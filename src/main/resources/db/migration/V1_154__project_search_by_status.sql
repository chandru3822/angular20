drop function if exists flow.search_projects_by_user(character varying, integer, integer, boolean, integer, integer);
drop function if exists flow.search_projects_with_down_line(character varying, integer, integer, boolean, integer, integer);
drop function if exists flow.search_all_projects(character varying, integer, boolean, integer, integer);

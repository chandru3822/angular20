<sql>

    <residual.getAll><![CDATA[

    ]]></residual.getAll>

    <residual.getOne><![CDATA[

    ]]></residual.getOne>

    <residual.insert><![CDATA[

    ]]></residual.insert>

    <residual.update><![CDATA[

    ]]></residual.update>

    <residual.delete><![CDATA[

    ]]></residual.delete>

    <residual.getAllPlans><![CDATA[
        select rp.id,
               rp.name,
               rp.residual_status_id,
               rps.status_type,
               rp.position_id,
               p.position,
               rp.approved,
               rp.description,
               rp.notes
        from brs.residual_plan rp
            inner join brs.residual_plan_status rps on rps.id = rp.residual_status_id
            inner join flow.position p on p.id = rp.position_id
    ]]></residual.getAllPlans>

    <residual.getResidualPlanDetails><![CDATA[
        SELECT row_to_json(sub_rows) sub_rows
        FROM (SELECT rp.id,
                     rps.status_type as "statusType",
                     rp.name,
                     rp.description,
                     rp.residual_status_id as "residualStatusId",
                     concat(cu.first_name, ' ', cu.last_name) AS "createdName",
                     concat(au.first_name, ' ', au.last_name) AS "approvedName",
                     rp.created_by as "createdBy",
                     rp.position_id as "positionId",
                     rp.approved                                                 AS "approvedDate",
                     coalesce((SELECT array_to_json(array_agg(row_to_json(allocations)))
                        FROM (
                          select rpa.id,
                                 rpa.residual_plan_id as "residualPlanId",
                                 rpa.name,
                                 rpa.level,
                                 rpa.total,
                                 rpa.nbr_fdc_lower as "nbrFdcLower",
                                 rpa.nbr_fdc_upper as "nbrFdcUpper"
                          from brs.residual_plan_allocation rpa
                          where rpa.residual_plan_id = rp.id
                          order by rpa.level
                      ) AS allocations), '[]') AS "residualPlanAllocations",
                     coalesce((SELECT array_to_json(array_agg(row_to_json(users)))
                               FROM (SELECT rpu.id,
                                            concat(u.first_name, ' ', u.last_name) AS name,
                                            string_agg(DISTINCT p.position, ', ') FILTER (WHERE
                                                CASE WHEN rp.position_id = 1
                                                         THEN up.id IN (select up5.id
                                                                from flow.user_position up5
                                                                         inner join flow.custom_field cf on up5.position_id = any(cf.system_list_option_ids) and cf.parent_custom_field_id = 9959
                                                                where up5.primary_flag is true
                                                                  and up5.archived is false
                                                                  and cf.archived is false)
                                                     WHEN rp.position_id = 4
                                                         THEN up.position_id IN (select unnest(string_to_array(value, ',')::int[])
                                                                                                        from flow.company_configuration_value
                                                                                                        where code = 'SETTER_POSITION_IDS')
                                                    END
                                                )                                  AS position,
                                            u.id                               AS "userId",
                                            rpu.start_date                     AS "startDate",
                                            rpu.end_date                       AS "endDate",
                                            false                              as "archived"
                                     FROM brs.residual_plan_user rpu
                                              INNER JOIN flow.user u ON rpu.user_id = u.id
                                              INNER JOIN flow.user_position up ON up.user_id = u.id
                                              INNER JOIN flow.position p ON p.id = up.position_id
                                     WHERE rpu.residual_plan_id = rp.id
                                     GROUP BY rpu.id, u.id) AS users), '[]')                    AS users
              FROM brs.residual_plan rp
                       INNER JOIN brs.residual_plan_status rps ON rp.residual_status_id = rps.id
                       LEFT JOIN flow.user cu ON rp.created_by = cu.id
                       LEFT JOIN flow.user au ON rp.approved_by = au.id
              WHERE rp.id = :planId) sub_rows;
    ]]></residual.getResidualPlanDetails>

    <residual.createPlan><![CDATA[
        insert into brs.residual_plan(name, residual_status_id, position_id, created, created_by, description)
        values(:name, 1, 1, now(), :createdBy, :description)
    ]]></residual.createPlan>

    <residual.updatePlan><![CDATA[
        update brs.residual_plan
            set name = :name,
                description = :description
        where id = :id
        returning id
    ]]></residual.updatePlan>

    <residual.getResidualPlanUserHistory><![CDATA[
        SELECT coalesce(array_to_json(array_agg(row_to_json(aup))), '[]')
        FROM (SELECT rpu.id as "userPlanId",
                     rpu.start_date  AS "startDate",
                     rpu.end_date    AS "endDate",
                     rp.name         ,
                     rps.status_type AS status,
                     rp.residual_status_id    AS residualStatusId,
                     rp.id
              FROM brs.residual_plan_user rpu
                       INNER JOIN brs.residual_plan rp ON rpu.residual_plan_id = rp.id
                       INNER JOIN brs.residual_plan_status rps ON rp.residual_status_id = rps.id
              WHERE rpu.user_id = :userId
              ORDER BY rpu.end_date DESC) aup;
    ]]></residual.getResidualPlanUserHistory>

    <residual.updatePlanUser><![CDATA[
        UPDATE brs.residual_plan_user
        SET start_date = :startDate,
          end_date     = :endDate
        WHERE id = :id
    ]]></residual.updatePlanUser>

    <residual.insertPlanEndDate><![CDATA[
        WITH active_commission_plans AS (SELECT rpu.id,
                                                rp.id   AS plan_id,
                                                rp.name AS plan_name,
                                                rpu.user_id,
                                                rpu.start_date,
                                                rpu.end_date
                                         FROM brs.residual_plan rp
                                                INNER JOIN brs.residual_plan_user rpu ON rp.id = rpu.residual_plan_id
                                         WHERE rp.residual_status_id <> 3
                                           AND rpu.end_date IS NULL
                                           AND rpu.start_date <= :startDate
                                           AND rpu.user_id = :userId)
        UPDATE brs.residual_plan_user rpu
        SET end_date = :startDate :: DATE - INTERVAL  '1 day'
        FROM active_commission_plans p
        WHERE rpu.id = p.id
    ]]></residual.insertPlanEndDate>

    <residual.insertPlanUser><![CDATA[
      INSERT INTO brs.residual_plan_user (residual_plan_id, user_id, start_date, end_date)
      VALUES (:planId, :userId, :startDate, :endDate)
    ]]></residual.insertPlanUser>

    <residual.getResidualPlanUsers><![CDATA[
        SELECT coalesce(array_to_json(array_agg(row_to_json(users))), '[]')
        FROM (SELECT rpu.id,
                     concat(u.first_name, ' ', u.last_name) AS name,
                     u.id                               AS "userId",
                     rpu.start_date                     AS "startDate",
                     rpu.end_date                       AS "endDate",
                     string_agg(DISTINCT p.position, ', ') FILTER (WHERE
                         CASE WHEN rp.position_id = 1
                                  THEN up.id IN (select up5.id
                                                  from flow.user_position up5
                                                           inner join flow.custom_field cf on up5.position_id = any(cf.system_list_option_ids) and cf.parent_custom_field_id = 9959
                                                  where up5.primary_flag is true
                                                    and up5.archived is false
                                                    and cf.archived is false)
                              WHEN rp.position_id = 4
                                  THEN up.position_id IN (select unnest(string_to_array(value, ',')::int[])
                                                                                                        from flow.company_configuration_value
                                                                                                        where code = 'SETTER_POSITION_IDS')
                             END
                         )                                  AS position
              FROM brs.residual_plan_user rpu
                       INNER JOIN brs.residual_plan rp ON rpu.residual_plan_id = rp.id
                       INNER JOIN flow.user u ON rpu.user_id = u.id
                       INNER JOIN flow.user_position up ON up.user_id = u.id
                       INNER JOIN flow.position p ON p.id = up.position_id
              WHERE rp.id = :planId
              GROUP BY rpu.id, u.id) AS users
    ]]></residual.getResidualPlanUsers>

    <residual.deletePlan><![CDATA[
        DELETE
          FROM brs.residual_plan_user
          WHERE residual_plan_id = :planId;

        DELETE
          FROM brs.residual_plan_allocation
       WHERE residual_plan_id = :planId;

         DELETE
          FROM brs.residual_plan
         WHERE id = :planId;
    ]]></residual.deletePlan>

    <residual.approvePlan><![CDATA[
        UPDATE brs.residual_plan
        SET approved  = now(),
          approved_by = :approvedBy,
          residual_status_id   = :statusId
        WHERE id = :planId
    ]]></residual.approvePlan>

    <residual.getResidualPlanAllocation><![CDATA[
    SELECT row_to_json(allocation) AS allocation
      FROM (select rpa.id,
         rpa.residual_plan_id as "residualPlanId",
         rpa.name,
         rpa.level,
         rpa.total,
         rpa.nbr_fdc_lower as "nbrFdcLower",
         rpa.nbr_fdc_upper as "nbrFdcUpper"
      from brs.residual_plan_allocation rpa
      where rpa.id = :id
    ) allocation
  ]]></residual.getResidualPlanAllocation>

    <residual.insertAllocation><![CDATA[
      INSERT INTO brs.residual_plan_allocation (name, level, total, residual_plan_id, nbr_fdc_lower, nbr_fdc_upper, created, created_by)
      VALUES (:name, :level, :total, :planId, :nbrFdcLower, :nbrFdcUpper, now(), :createdById)
    ]]></residual.insertAllocation>

    <residual.updateAllocation><![CDATA[
      UPDATE brs.residual_plan_allocation
       SET  name = :name,
            level = :level,
            total = :total,
            nbr_fdc_lower = :nbrFdcLower,
            nbr_fdc_upper = :nbrFdcUpper
       WHERE id = :id
    ]]></residual.updateAllocation>

    <residual.removeAllocation><![CDATA[
        DELETE
      FROM brs.residual_plan_allocation
       WHERE id = :id
    ]]></residual.removeAllocation>

    <residual.clonePlan><![CDATA[
        SELECT clone_residual_plan AS id
        FROM brs.clone_residual_plan(:planId::int, :startDate::DATE, :users::int[], :createdBy::int)
    ]]></residual.clonePlan>
</sql>

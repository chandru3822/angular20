<sql>
    <dash.getTargets><![CDATA[
        select *
        from brs.company_dashboard_targets
        order by target_date desc
    ]]></dash.getTargets>

    <dash.insertTargets><![CDATA[
        insert into brs.company_dashboard_targets (
            target_date, bookings_brs, bookings_partner, final_designs_completed_brs,
            final_designs_completed_partner, substantial_completions_brs, substantial_completions_partner,
            final_completions_brs,  final_completions_partner
        )
        values (
            :targetDate, :bookingsBrs, :bookingsPartner, :finalDesignsCompletedBrs,
            :finalDesignsCompletedPartner, :substantialCompletionsBrs, :substantialCompletionsPartner,
            :finalCompletionsBrs, :finalCompletionsPartner
        )
        on conflict do nothing
    ]]></dash.insertTargets>

    <dash.updateTargets><![CDATA[
        update brs.company_dashboard_targets
        set bookings_brs = :bookingsBrs,
            bookings_partner = :bookingsPartner,
            final_designs_completed_brs = :finalDesignsCompletedBrs,
            final_designs_completed_partner = :finalDesignsCompletedPartner,
            substantial_completions_brs = :substantialCompletionsBrs,
            substantial_completions_partner = :substantialCompletionsPartner,
            final_completions_brs = :finalCompletionsBrs,
            final_completions_partner = :finalCompletionsPartner
        where id = :id
    ]]></dash.updateTargets>

    <dash.getPlannedValues><![CDATA[
        select *
        from brs.company_dashboard_targets
        where target_date BETWEEN :startDate::DATE and :endDate::DATE
    ]]></dash.getPlannedValues>

    <dash.getFirstTimeAppointmentsCreatedActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        ), first_appointment as (
            select pps.project_id,
                   min(pps.process_step_complete_date) date_created
            from flow.project_process_step pps
            where pps.process_step_id = 1
                and pps.process_step_complete_date is not null
            group by pps.project_id
        )
        select count(1)
        from companies, brs.project_details pd
            inner join first_appointment fa on fa.project_id = pd.project_id
        where ((fa.date_created at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getFirstTimeAppointmentsCreatedActualTotal>

    <dash.getFirstTimeAppointmentsCreatedActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        ), first_appointment as (
            select pps.project_id,
                   min(pps.process_step_complete_date) date_created
            from flow.project_process_step pps
            where pps.process_step_id = 1
                and pps.process_step_complete_date is not null
            group by pps.project_id
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               fa.date_created appointment_created_date
        from companies, brs.project_details pd
            inner join first_appointment fa on fa.project_id = pd.project_id
            inner join flow.project p on p.id = fa.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((fa.date_created at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getFirstTimeAppointmentsCreatedActualTotalDrilldown>

    <dash.getFirstTimeAppointmentsCreatedActualBrs><![CDATA[
        with first_appointment as (
            select pps.project_id,
                   min(pps.process_step_complete_date) date_created
            from flow.project_process_step pps
            where pps.process_step_id = 1
                and pps.process_step_complete_date is not null
            group by pps.project_id
        )
        select count(1)
        from brs.project_details pd
            inner join first_appointment fa on fa.project_id = pd.project_id
        where ((fa.date_created at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getFirstTimeAppointmentsCreatedActualBrs>

    <dash.getFirstTimeAppointmentsCreatedActualBrsDrilldown><![CDATA[
        with first_appointment as (
            select pps.project_id,
                   min(pps.process_step_complete_date) date_created
            from flow.project_process_step pps
            where pps.process_step_id = 1
                and pps.process_step_complete_date is not null
            group by pps.project_id
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               fa.date_created appointment_created_date
        from brs.project_details pd
            inner join first_appointment fa on fa.project_id = pd.project_id
            inner join flow.project p on p.id = fa.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((fa.date_created at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getFirstTimeAppointmentsCreatedActualBrsDrilldown>

    <dash.getFirstTimeAppointmentsCreatedActualPartner><![CDATA[
        with first_appointment as (
            select pps.project_id,
                   min(pps.process_step_complete_date) date_created
            from flow.project_process_step pps
            where pps.process_step_id = 1
                and pps.process_step_complete_date is not null
            group by pps.project_id
        )
        select count(1)
        from brs.project_details pd
            inner join first_appointment fa on fa.project_id = pd.project_id
        where ((fa.date_created at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getFirstTimeAppointmentsCreatedActualPartner>

    <dash.getFirstTimeAppointmentsCreatedActualPartnerDrilldown><![CDATA[
        with first_appointment as (
            select pps.project_id,
                   min(pps.process_step_complete_date) date_created
            from flow.project_process_step pps
            where pps.process_step_id = 1
                and pps.process_step_complete_date is not null
            group by pps.project_id
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               fa.date_created appointment_created_date
        from brs.project_details pd
            inner join first_appointment fa on fa.project_id = pd.project_id
            inner join flow.project p on p.id = fa.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((fa.date_created at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getFirstTimeAppointmentsCreatedActualPartnerDrilldown>

    <dash.getPlannedAppointmentsActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where ((pd.first_appointment at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPlannedAppointmentsActualTotal>

    <dash.getPlannedAppointmentsActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.first_appointment appointment_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((pd.first_appointment at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPlannedAppointmentsActualTotalDrilldown>

    <dash.getPlannedAppointmentsActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where ((pd.first_appointment at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getPlannedAppointmentsActualBrs>

    <dash.getPlannedAppointmentsActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.first_appointment appointment_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((pd.first_appointment at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getPlannedAppointmentsActualBrsDrilldown>

    <dash.getPlannedAppointmentsActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where ((pd.first_appointment at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getPlannedAppointmentsActualPartner>

    <dash.getPlannedAppointmentsActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.first_appointment appointment_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((pd.first_appointment at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getPlannedAppointmentsActualPartnerDrilldown>

    <dash.getPitchedActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where ((pd.first_appointment_pitched at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPitchedActualTotal>

    <dash.getPitchedActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.first_appointment_pitched appointment_date,
               lov.name appointment_outcome
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            inner join flow.list_of_value lov on lov.id = pd.first_appointment_pitched_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((pd.first_appointment_pitched at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPitchedActualTotalDrilldown>

    <dash.getPitchedActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.company_id = 3
            and ((pd.first_appointment_pitched at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
    ]]></dash.getPitchedActualBrs>

    <dash.getPitchedActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.first_appointment_pitched appointment_date,
               lov.name appointment_outcome
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            inner join flow.list_of_value lov on lov.id = pd.first_appointment_pitched_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.company_id = 3
            and ((pd.first_appointment_pitched at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
    ]]></dash.getPitchedActualBrsDrilldown>

    <dash.getPitchedActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.company_id != 3
            and ((pd.first_appointment_pitched at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
    ]]></dash.getPitchedActualPartner>

    <dash.getPitchedActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.first_appointment_pitched appointment_date,
               lov.name appointment_outcome
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            inner join flow.list_of_value lov on lov.id = pd.first_appointment_pitched_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.company_id != 3
            and ((pd.first_appointment_pitched at time zone 'UTC') at time zone 'US/Mountain') :: date between :startDate::DATE and :endDate::DATE
    ]]></dash.getPitchedActualPartnerDrilldown>

    <dash.getBookingsActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where pd.installation_agreement_signed_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getBookingsActualTotal>

    <dash.getBookingsActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.installation_agreement_signed_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.installation_agreement_signed_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getBookingsActualTotalDrilldown>

    <dash.getBookingsActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.installation_agreement_signed_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getBookingsActualBrs>

    <dash.getBookingsActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.installation_agreement_signed_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.installation_agreement_signed_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getBookingsActualBrsDrilldown>

    <dash.getBookingsActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.installation_agreement_signed_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getBookingsActualPartner>

    <dash.getBookingsActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.installation_agreement_signed_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.installation_agreement_signed_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getBookingsActualPartnerDrilldown>

    <dash.getSiteSurveysVerifiedActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where pd.site_survey_verified_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getSiteSurveysVerifiedActualTotal>

    <dash.getSiteSurveysVerifiedActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.site_survey_verified_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.site_survey_verified_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getSiteSurveysVerifiedActualTotalDrilldown>

    <dash.getSiteSurveysVerifiedActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.site_survey_verified_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getSiteSurveysVerifiedActualBrs>

    <dash.getSiteSurveysVerifiedActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.site_survey_verified_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.site_survey_verified_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getSiteSurveysVerifiedActualBrsDrilldown>

    <dash.getSiteSurveysVerifiedActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.site_survey_verified_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getSiteSurveysVerifiedActualPartner>

    <dash.getSiteSurveysVerifiedActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.site_survey_verified_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.site_survey_verified_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getSiteSurveysVerifiedActualPartnerDrilldown>

    <dash.getFinalDesignsCreatedActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where ((pd.final_design_created_timestamp at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getFinalDesignsCreatedActualTotal>

    <dash.getFinalDesignsCreatedActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_design_created_timestamp final_design_created_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((pd.final_design_created_timestamp at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getFinalDesignsCreatedActualTotalDrilldown>

    <dash.getFinalDesignsCreatedActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where ((pd.final_design_created_timestamp at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getFinalDesignsCreatedActualBrs>

    <dash.getFinalDesignsCreatedActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_design_created_timestamp final_design_created_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((pd.final_design_created_timestamp at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getFinalDesignsCreatedActualBrsDrilldown>

    <dash.getFinalDesignsCreatedActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where ((pd.final_design_created_timestamp at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getFinalDesignsCreatedActualPartner>

    <dash.getFinalDesignsCreatedActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_design_created_timestamp final_design_created_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((pd.final_design_created_timestamp at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getFinalDesignsCreatedActualPartnerDrilldown>

    <dash.getFinalDesignsSentActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where ((pd.final_design_sent_to_homeowner_date at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getFinalDesignsSentActualTotal>

    <dash.getFinalDesignsSentActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_design_sent_to_homeowner_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((pd.final_design_sent_to_homeowner_date at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getFinalDesignsSentActualTotalDrilldown>

    <dash.getFinalDesignsSentActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where ((pd.final_design_sent_to_homeowner_date at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getFinalDesignsSentActualBrs>

    <dash.getFinalDesignsSentActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_design_sent_to_homeowner_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((pd.final_design_sent_to_homeowner_date at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getFinalDesignsSentActualBrsDrilldown>

    <dash.getFinalDesignsSentActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where ((pd.final_design_sent_to_homeowner_date at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getFinalDesignsSentActualPartner>

    <dash.getFinalDesignsSentActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_design_sent_to_homeowner_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((pd.final_design_sent_to_homeowner_date at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getFinalDesignsSentActualPartnerDrilldown>

    <dash.getFinalDesignsApprovedActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where pd.final_design_signed_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getFinalDesignsApprovedActualTotal>

    <dash.getFinalDesignsApprovedActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_design_signed_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.final_design_signed_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getFinalDesignsApprovedActualTotalDrilldown>

    <dash.getFinalDesignsApprovedActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.final_design_signed_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getFinalDesignsApprovedActualBrs>

    <dash.getFinalDesignsApprovedActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_design_signed_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.final_design_signed_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getFinalDesignsApprovedActualBrsDrilldown>

    <dash.getFinalDesignsApprovedActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.final_design_signed_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getFinalDesignsApprovedActualPartner>

    <dash.getFinalDesignsApprovedActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_design_signed_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.final_design_signed_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getFinalDesignsApprovedActualPartnerDrilldown>

    <dash.getFinalDesignsCompletedActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where pd.final_design_complete_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getFinalDesignsCompletedActualTotal>

    <dash.getFinalDesignsCompletedActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_design_complete_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.final_design_complete_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getFinalDesignsCompletedActualTotalDrilldown>

    <dash.getFinalDesignsCompletedActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.final_design_complete_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getFinalDesignsCompletedActualBrs>

    <dash.getFinalDesignsCompletedActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_design_complete_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.final_design_complete_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getFinalDesignsCompletedActualBrsDrilldown>

    <dash.getFinalDesignsCompletedActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.final_design_complete_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getFinalDesignsCompletedActualPartner>

    <dash.getFinalDesignsCompletedActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_design_complete_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.final_design_complete_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getFinalDesignsCompletedActualPartnerDrilldown>

    <dash.getPlanSetsCreatedActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where pd.plan_set_created_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPlanSetsCreatedActualTotal>

    <dash.getPlanSetsCreatedActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.plan_set_created_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.plan_set_created_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPlanSetsCreatedActualTotalDrilldown>

    <dash.getPlanSetsCreatedActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.plan_set_created_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getPlanSetsCreatedActualBrs>

    <dash.getPlanSetsCreatedActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.plan_set_created_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.plan_set_created_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getPlanSetsCreatedActualBrsDrilldown>

    <dash.getPlanSetsCreatedActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.plan_set_created_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getPlanSetsCreatedActualPartner>

    <dash.getPlanSetsCreatedActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.plan_set_created_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.plan_set_created_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getPlanSetsCreatedActualPartnerDrilldown>

    <dash.getPermitPacksCreatedActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where pd.permit_pack_complete BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPermitPacksCreatedActualTotal>

    <dash.getPermitPacksCreatedActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.permit_pack_complete permit_pack_complete_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.permit_pack_complete BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPermitPacksCreatedActualTotalDrilldown>

    <dash.getPermitPacksCreatedActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.permit_pack_complete BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getPermitPacksCreatedActualBrs>

    <dash.getPermitPacksCreatedActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.permit_pack_complete permit_pack_complete_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.permit_pack_complete BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getPermitPacksCreatedActualBrsDrilldown>

    <dash.getPermitPacksCreatedActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.permit_pack_complete BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getPermitPacksCreatedActualPartner>

    <dash.getPermitPacksCreatedActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.permit_pack_complete permit_pack_complete_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.permit_pack_complete BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getPermitPacksCreatedActualPartnerDrilldown>

    <dash.getPermitsSubmittedActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where ((pd.permit_pack_submittal_end_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPermitsSubmittedActualTotal>

    <dash.getPermitsSubmittedActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.permit_pack_submittal_end_time permit_submitted_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((pd.permit_pack_submittal_end_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPermitsSubmittedActualTotalDrilldown>

    <dash.getPermitsSubmittedActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where ((pd.permit_pack_submittal_end_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getPermitsSubmittedActualBrs>

    <dash.getPermitsSubmittedActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.permit_pack_submittal_end_time permit_submitted_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((pd.permit_pack_submittal_end_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getPermitsSubmittedActualBrsDrilldown>

    <dash.getPermitsSubmittedActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where ((pd.permit_pack_submittal_end_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getPermitsSubmittedActualPartner>

    <dash.getPermitsSubmittedActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.permit_pack_submittal_end_time permit_submitted_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where ((pd.permit_pack_submittal_end_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getPermitsSubmittedActualPartnerDrilldown>

    <dash.getPermitsApprovedActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where pd.permit_approved_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPermitsApprovedActualTotal>

    <dash.getPermitsApprovedActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.permit_approved_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.permit_approved_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPermitsApprovedActualTotalDrilldown>

    <dash.getPermitsApprovedActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.permit_approved_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getPermitsApprovedActualBrs>

    <dash.getPermitsApprovedActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.permit_approved_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.permit_approved_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getPermitsApprovedActualBrsDrilldown>

    <dash.getPermitsApprovedActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.permit_approved_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getPermitsApprovedActualPartner>

    <dash.getPermitsApprovedActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.permit_approved_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.permit_approved_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getPermitsApprovedActualPartnerDrilldown>

    <dash.getInstallationsScheduledActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where pd.installation_scheduled BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getInstallationsScheduledActualTotal>

    <dash.getInstallationsScheduledActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.installation_scheduled installation_scheduled_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.installation_scheduled BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getInstallationsScheduledActualTotalDrilldown>

    <dash.getInstallationsScheduledActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.installation_scheduled BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getInstallationsScheduledActualBrs>

    <dash.getInstallationsScheduledActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.installation_scheduled installation_scheduled_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.installation_scheduled BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getInstallationsScheduledActualBrsDrilldown>

    <dash.getInstallationsScheduledActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.installation_scheduled BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getInstallationsScheduledActualPartner>

    <dash.getInstallationsScheduledActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.installation_scheduled installation_scheduled_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.installation_scheduled BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getInstallationsScheduledActualPartnerDrilldown>

    <dash.getPlannedInstallationsActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where (((pd.installation_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE or
            ((pd.installation_closeout_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE)
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPlannedInstallationsActualTotal>

    <dash.getPlannedInstallationsActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.installation_start_time installation_date,
               pd.installation_closeout_start_time installation_closeout_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where (((pd.installation_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE or
            ((pd.installation_closeout_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE)
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPlannedInstallationsActualTotalDrilldown>

    <dash.getPlannedInstallationsActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where (((pd.installation_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE or
            ((pd.installation_closeout_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id = 3
    ]]></dash.getPlannedInstallationsActualBrs>

    <dash.getPlannedInstallationsActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.installation_start_time installation_date,
               pd.installation_closeout_start_time installation_closeout_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where (((pd.installation_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE or
            ((pd.installation_closeout_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id = 3
    ]]></dash.getPlannedInstallationsActualBrsDrilldown>

    <dash.getPlannedInstallationsActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where (((pd.installation_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE or
            ((pd.installation_closeout_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id != 3
    ]]></dash.getPlannedInstallationsActualPartner>

    <dash.getPlannedInstallationsActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.installation_start_time installation_date,
               pd.installation_closeout_start_time installation_closeout_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where (((pd.installation_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE or
            ((pd.installation_closeout_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id != 3
    ]]></dash.getPlannedInstallationsActualPartnerDrilldown>

    <dash.getSubstantialCompletionsActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where pd.substantial_completion_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getSubstantialCompletionsActualTotal>

    <dash.getSubstantialCompletionsActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.substantial_completion_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.substantial_completion_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getSubstantialCompletionsActualTotalDrilldown>

    <dash.getSubstantialCompletionsActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.substantial_completion_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getSubstantialCompletionsActualBrs>

    <dash.getSubstantialCompletionsActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.substantial_completion_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.substantial_completion_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getSubstantialCompletionsActualBrsDrilldown>

    <dash.getSubstantialCompletionsActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.substantial_completion_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getSubstantialCompletionsActualPartner>

    <dash.getSubstantialCompletionsActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.substantial_completion_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.substantial_completion_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getSubstantialCompletionsActualPartnerDrilldown>

    <dash.getInspectionsScheduledActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where (pd.ahj_inspection_scheduled_date BETWEEN :startDate::DATE and :endDate::DATE or
            pd.ahj_reinspection_scheduled_date BETWEEN :startDate::DATE and :endDate::DATE)
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getInspectionsScheduledActualTotal>

    <dash.getInspectionsScheduledActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.ahj_inspection_scheduled_date,
               pd.ahj_reinspection_scheduled_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where (pd.ahj_inspection_scheduled_date BETWEEN :startDate::DATE and :endDate::DATE or
            pd.ahj_reinspection_scheduled_date BETWEEN :startDate::DATE and :endDate::DATE)
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getInspectionsScheduledActualTotalDrilldown>

    <dash.getInspectionsScheduledActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where (pd.ahj_inspection_scheduled_date BETWEEN :startDate::DATE and :endDate::DATE or
            pd.ahj_reinspection_scheduled_date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id = 3
    ]]></dash.getInspectionsScheduledActualBrs>

    <dash.getInspectionsScheduledActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.ahj_inspection_scheduled_date,
               pd.ahj_reinspection_scheduled_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where (pd.ahj_inspection_scheduled_date BETWEEN :startDate::DATE and :endDate::DATE or
            pd.ahj_reinspection_scheduled_date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id = 3
    ]]></dash.getInspectionsScheduledActualBrsDrilldown>

    <dash.getInspectionsScheduledActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where (pd.ahj_inspection_scheduled_date BETWEEN :startDate::DATE and :endDate::DATE or
            pd.ahj_reinspection_scheduled_date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id != 3
    ]]></dash.getInspectionsScheduledActualPartner>

    <dash.getInspectionsScheduledActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.ahj_inspection_scheduled_date,
               pd.ahj_reinspection_scheduled_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where (pd.ahj_inspection_scheduled_date BETWEEN :startDate::DATE and :endDate::DATE or
            pd.ahj_reinspection_scheduled_date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id != 3
    ]]></dash.getInspectionsScheduledActualPartnerDrilldown>

    <dash.getPlannedInspectionsActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where (((pd.ahj_inspection_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE or
            ((pd.ahj_reinspection_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE)
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPlannedInspectionsActualTotal>

    <dash.getPlannedInspectionsActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.ahj_inspection_start_time ahj_inspection_date,
               pd.ahj_reinspection_start_time ahj_reinspection_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where (((pd.ahj_inspection_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE or
            ((pd.ahj_reinspection_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE)
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getPlannedInspectionsActualTotalDrilldown>

    <dash.getPlannedInspectionsActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where (((pd.ahj_inspection_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE or
            ((pd.ahj_reinspection_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id = 3
    ]]></dash.getPlannedInspectionsActualBrs>

    <dash.getPlannedInspectionsActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.ahj_inspection_start_time ahj_inspection_date,
               pd.ahj_reinspection_start_time ahj_reinspection_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where (((pd.ahj_inspection_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE or
            ((pd.ahj_reinspection_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id = 3
    ]]></dash.getPlannedInspectionsActualBrsDrilldown>

    <dash.getPlannedInspectionsActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where (((pd.ahj_inspection_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE or
            ((pd.ahj_reinspection_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id != 3
    ]]></dash.getPlannedInspectionsActualPartner>

    <dash.getPlannedInspectionsActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.ahj_inspection_start_time ahj_inspection_date,
               pd.ahj_reinspection_start_time ahj_reinspection_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where (((pd.ahj_inspection_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE or
            ((pd.ahj_reinspection_start_time at time zone 'UTC') at time zone 'US/Mountain') :: date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id != 3
    ]]></dash.getPlannedInspectionsActualPartnerDrilldown>

    <dash.getInspectionsPassedActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where pd.ahj_final_inspection_verified BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getInspectionsPassedActualTotal>

    <dash.getInspectionsPassedActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.ahj_final_inspection_verified ahj_final_inspection_verified_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.ahj_final_inspection_verified BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getInspectionsPassedActualTotalDrilldown>

    <dash.getInspectionsPassedActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.ahj_final_inspection_verified BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getInspectionsPassedActualBrs>

    <dash.getInspectionsPassedActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.ahj_final_inspection_verified ahj_final_inspection_verified_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.ahj_final_inspection_verified BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getInspectionsPassedActualBrsDrilldown>

    <dash.getInspectionsPassedActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.ahj_final_inspection_verified BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getInspectionsPassedActualPartner>

    <dash.getInspectionsPassedActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.ahj_final_inspection_verified ahj_final_inspection_verified_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.ahj_final_inspection_verified BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getInspectionsPassedActualPartnerDrilldown>

    <dash.getInspectionResultsSubmittedActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where pd.verified_inspection_approval_received_by_utility_date BETWEEN :startDate::DATE and :endDate::DATE or
            pd.ahj_inspection_approval_submitted_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getInspectionResultsSubmittedActualTotal>

    <dash.getInspectionResultsSubmittedActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.verified_inspection_approval_received_by_utility_date,
               pd.ahj_inspection_approval_submitted_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.verified_inspection_approval_received_by_utility_date BETWEEN :startDate::DATE and :endDate::DATE or
            pd.ahj_inspection_approval_submitted_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getInspectionResultsSubmittedActualTotalDrilldown>

    <dash.getInspectionResultsSubmittedActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where (pd.verified_inspection_approval_received_by_utility_date BETWEEN :startDate::DATE and :endDate::DATE or
            pd.ahj_inspection_approval_submitted_date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id = 3
    ]]></dash.getInspectionResultsSubmittedActualBrs>

    <dash.getInspectionResultsSubmittedActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.verified_inspection_approval_received_by_utility_date,
               pd.ahj_inspection_approval_submitted_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where (pd.verified_inspection_approval_received_by_utility_date BETWEEN :startDate::DATE and :endDate::DATE or
            pd.ahj_inspection_approval_submitted_date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id = 3
    ]]></dash.getInspectionResultsSubmittedActualBrsDrilldown>

    <dash.getInspectionResultsSubmittedActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where (pd.verified_inspection_approval_received_by_utility_date BETWEEN :startDate::DATE and :endDate::DATE or
            pd.ahj_inspection_approval_submitted_date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id != 3
    ]]></dash.getInspectionResultsSubmittedActualPartner>

    <dash.getInspectionResultsSubmittedActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.verified_inspection_approval_received_by_utility_date,
               pd.ahj_inspection_approval_submitted_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where (pd.verified_inspection_approval_received_by_utility_date BETWEEN :startDate::DATE and :endDate::DATE or
            pd.ahj_inspection_approval_submitted_date BETWEEN :startDate::DATE and :endDate::DATE)
            and pd.company_id != 3
    ]]></dash.getInspectionResultsSubmittedActualPartnerDrilldown>

    <dash.getFinalCompletionsActualTotal><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select count(1)
        from companies, brs.project_details pd
        where pd.final_completion_submitted_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getFinalCompletionsActualTotal>

    <dash.getFinalCompletionsActualTotalDrilldown><![CDATA[
        with companies as (
            select array_agg(id) as ids
            from flow.company_hierarchy_filter_down(:parentCompanyId::int)
        )
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_completion_submitted_date
        from companies, brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.final_completion_submitted_date BETWEEN :startDate::DATE and :endDate::DATE
            and case when :isParent
                then pd.company_id = any(companies.ids)
                else pd.company_id = :companyId
                end
    ]]></dash.getFinalCompletionsActualTotalDrilldown>

    <dash.getFinalCompletionsActualBrs><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.final_completion_submitted_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getFinalCompletionsActualBrs>

    <dash.getFinalCompletionsActualBrsDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_completion_submitted_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.final_completion_submitted_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id = 3
    ]]></dash.getFinalCompletionsActualBrsDrilldown>

    <dash.getFinalCompletionsActualPartner><![CDATA[
        select count(1)
        from brs.project_details pd
        where pd.final_completion_submitted_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getFinalCompletionsActualPartner>

    <dash.getFinalCompletionsActualPartnerDrilldown><![CDATA[
        select concat(c.first_name, ' ', c.last_name) customer_name,
               pd.project_id,
               s.abbreviation state,
               pd.source_name,
               pd.final_completion_submitted_date
        from brs.project_details pd
            inner join flow.project p on p.id = pd.project_id
            inner join flow.contact c on c.id = p.contact_id
            left outer join flow.company_state cs on cs.id = c.company_state_id
            left outer join flow.state s on s.id = cs.state_id
        where pd.final_completion_submitted_date BETWEEN :startDate::DATE and :endDate::DATE
            and pd.company_id != 3
    ]]></dash.getFinalCompletionsActualPartnerDrilldown>
</sql>

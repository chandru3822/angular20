<sql>
  <customFieldGroup.insertCustomFieldGroup><![CDATA[
    insert into flow.custom_field_group(group_name, company_object_type_id, group_order, process_step_id, event_id, created_by_id, date_created, modified_by_id, date_modified)
    values (:groupName, :companyObjectTypeId,
    (select coalesce(max(group_order) + 1, 0) from flow.custom_field_group where case when :processStepId::int is not null then process_step_id = :processStepId::int else company_object_type_id = :companyObjectTypeId end),
    :processStepId, :eventId, :createdById, now(), :createdById, now())
  ]]></customFieldGroup.insertCustomFieldGroup>

  <customFieldGroup.getCompanyObjectTypeId><![CDATA[
    select id
    from flow.company_object_type
    where object_type_id = :objectTypeId
      and company_id = :companyId
  ]]></customFieldGroup.getCompanyObjectTypeId>

  <customFieldGroup.updateCustomFieldGroup><![CDATA[
    update flow.custom_field_group
      set group_name = :groupName,
          company_object_type_tab_id = :companyObjectTypeTabId,
          group_order = :groupOrder,
          modified_by_id = :modifiedById,
          date_modified = now()
    where id = :id
  ]]></customFieldGroup.updateCustomFieldGroup>

  <customFieldGroup.deleteCustomFieldGroup><![CDATA[
    update flow.custom_field_group
      set archived = true,
      modified_by_id = :modifiedById,
      date_modified = now()
    where id = :id
  ]]></customFieldGroup.deleteCustomFieldGroup>

  <customFieldGroup.getUserProfileFields><![CDATA[

  ]]></customFieldGroup.getUserProfileFields>

  <customFieldGroup.getInsertFieldsByType><![CDATA[
    select cfg.id,
           cfg.group_name as "groupName",
           cfg.group_order as "groupOrder",
           coalesce((
                      SELECT array_to_json(array_agg(row_to_json(fields)))
                      FROM (
                             select null as id,
                                    cfga.custom_field_group_id as "customFieldGroupId",
                                    cfga.id as "customFieldGroupAssignmentId",
                                    cfga.field_order as "fieldOrder",
                                    cfga.read_only as "customFieldGroupAssignmentReadOnly",
                                    cfga.hidden as "customFieldGroupAssignmentHidden",
                                    cf.readonly,
                                    cf.list_of_value_id as "listOfValueId",
                                    cf.company_system_list_id as "companySystemListId",
                                    cf.system_list_option_ids as "systemListOptionIds",
                                    cf.id as "customFieldId",
                                    cf.field_name as "fieldName",
                                    cf.sort_list_values_alphabetically as "sortListValuesAlphabetically",
                                    cf.custom_field_sql_key as "customFieldSqlKey",
                                    cf.company_data_type_id as "companyDataTypeId",
                                    cfga.show_on_insert as "showOnInsert",
                                    cfga.show_on_user_profile as "showOnUserProfile",
                                    cfga.require_on_insert as "requireOnInsert",
                                    cdt.data_type_id as "dataTypeId",
                                    cdt.has_list_values as "hasListValues",
                                    coalesce((
                                               SELECT array_to_json(array_agg(row_to_json(listOfValues)))
                                               FROM (
                                                      select lov.id,
                                                             lov.name,
                                                             lov.code,
                                                             lov.parent_id,
                                                             lov.display_order
                                                      from flow.list_of_value lov
                                                      where lov.parent_id is not null
                                                        and lov.parent_id = cf.list_of_value_id
                                                        and lov.archived is not true
                                                      order by
                                                         case when cf.sort_list_values_alphabetically is true  then lov.name end,
                                                         case when cf.sort_list_values_alphabetically is false then lov.display_order end
                                                    ) listOfValues), '[]') AS "listOfValues",
                                    coalesce((
                                               SELECT array_to_json(array_agg(row_to_json(wlp)))
                                               FROM (
                                                      SELECT wlp.id,
                                                             wlp.position_id as "positionId",
                                                             wlp.custom_field_group_assignment_id as "custom_field_group_assignment_id",
                                                             wlp.created_by_id as "createdById",
                                                             wlp.modified_by_id as "modifiedById",
                                                             wlp.archived
                                                      FROM flow.white_listed_position wlp
                                                      WHERE wlp.custom_field_group_assignment_id = cfga.id
                                                        AND wlp.white_list_type_id = 1
                                                        AND wlp.archived is not true) wlp), '[]') AS "whiteListedPositions",
                                    coalesce((
                                               SELECT array_to_json(array_agg(row_to_json(wlp)))
                                               FROM (
                                                      SELECT wlp.id,
                                                             wlp.position_id as "positionId",
                                                             wlp.custom_field_group_assignment_id as "custom_field_group_assignment_id",
                                                             wlp.created_by_id as "createdById",
                                                             wlp.modified_by_id as "modifiedById",
                                                             wlp.archived
                                                      FROM flow.white_listed_position wlp
                                                      WHERE wlp.custom_field_group_assignment_id = cfga.id
                                                        AND wlp.white_list_type_id = 2
                                                        AND wlp.archived is not true) wlp), '[]') AS "hiddenWhiteListedPositions"
                             from flow.custom_field_group_assignment cfga
                                    inner join flow.custom_field cf on cf.id = cfga.custom_field_id
                                    inner join flow.company_data_type cdt on cdt.id = cf.company_data_type_id
                             where cfga.custom_field_group_id = cfg.id
                               and cfga.archived is not true
                               and cf.company_id = :companyId
                               and cfga.show_on_insert is true
                             order by cfga.field_order, cf.field_name
                           ) fields), '[]') AS "customFieldValues"
    from flow.custom_field_group cfg
           inner join flow.company_object_type cot on cot.id = cfg.company_object_type_id
    where cot.object_type_id = :objectTypeId
      and cfg.archived is not true
      and cot.company_id = :companyId
    order by cfg.group_order
  ]]></customFieldGroup.getInsertFieldsByType>

  <customFieldGroup.updateFieldShowOrRequire><![CDATA[
    update flow.custom_field_group_assignment
      set show_on_insert = :showOnInsert,
        show_on_user_profile = :showOnUserProfile,
        require_on_insert = :requireOnInsert,
        modified_by_id = :modifiedById,
        date_modified = now()
    where id = :customFieldGroupAssignmentId
  ]]></customFieldGroup.updateFieldShowOrRequire>

  <customFieldGroup.checkForFieldsInUse><![CDATA[
    select null as field_name,
           null as group_name,
           ps.process_step_name,
           'Requirement' as object_type
    from flow.process_step_requirement psr
             inner join flow.custom_field_group_assignment cfga on cfga.id = psr.custom_field_group_assignment_id
             inner join flow.process_step ps on ps.id = psr.process_step_id
    where case when :processStepId::int is not null then ps.id != :processStepId:: int and cfga.id in (
        select cfga4.id
        from flow.custom_field_group cfg2
                 inner join flow.custom_field_group_assignment cfga4 on cfga4.custom_field_group_id = cfg2.id
        where cfg2.process_step_id = :processStepId::int                  )
               when :customFieldGroupAssignmentId::int is not null then cfga.id = :customFieldGroupAssignmentId::int
        else cfga.custom_field_group_id = :customFieldGroupId end
        and psr.archived is not true
          and ps.archived is not true
        union all
    select cf.field_name,
           cfg.group_name,
           ps.process_step_name,
           ot.object_type
    from flow.custom_field_group_assignment cfga
             inner join flow.custom_field_group_assignment cfga1 on cfga1.id = cfga.ancillary_custom_field_group_assignment_id
             inner join flow.custom_field cf on cf.id = cfga1.custom_field_id
             inner join flow.custom_field_group cfg on cfg.id = cfga.custom_field_group_id
             left join flow.process_step ps on ps.id = cfg.process_step_id
             inner join flow.company_object_type cot on cot.id = cfg.company_object_type_id
             inner join flow.object_type ot on ot.id = cot.object_type_id
    where case when :processStepId::int is not null then ps.id != :processStepId:: int and cfga1.id in (
        select cfga3.id
        from flow.custom_field_group cfg3
                 inner join flow.custom_field_group_assignment cfga3 on cfga3.custom_field_group_id = cfg3.id
        where cfg3.process_step_id = :processStepId::int                  )
               when :customFieldGroupAssignmentId::int is not null then cfga1.id = :customFieldGroupAssignmentId::int
        else cfga1.custom_field_group_id = :customFieldGroupId end
        and cfga1.archived is not true
        and cfg.archived is not true
        and cfga.archived is not true
        and ps.archived is not true
        order by object_type, process_step_name, group_name, field_name
  ]]></customFieldGroup.checkForFieldsInUse>
</sql>

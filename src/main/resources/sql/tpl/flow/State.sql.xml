<sql>
  <state.getAllStates><![CDATA[
    select s.id,
           s.state,
           s.active_flag,
           s.abbreviation,
           s.time_zone_abbreviation,
           s.map_latitude,
           s.map_longitude,
           s.map_zoom
    from blueraven.state s
    order by s.state
  ]]></state.getAllStates>

</sql>

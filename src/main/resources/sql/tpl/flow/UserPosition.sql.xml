<sql>
  <userPosition.getAll><![CDATA[
      select distinct upv.user_position_id as id,
                upv.user_id,
                upv.archived,
                upv.primary_flag,
                upv.start_date,
                upv.end_date,
                upv.position,
                upv.position_id,
                upv.company_id,
                uphv.hierarchy,
                p.use_slot_schedule
      from flow.user_positions_vw upv
        inner join flow.user_position_hierarchy_vw uphv on uphv.user_id = upv.user_id and uphv.org_id = upv.org_id and uphv.position_id = upv.position_id
        inner join flow.position p on p.id = upv.position_id
      where upv.company_id = :companyId
        and upv.user_id = :userId
        and upv.archived is not true
      order by upv.start_date desc
  ]]></userPosition.getAll>

  <userPosition.getAllActive><![CDATA[
      select distinct upv.user_position_id as id,
                upv.user_id,
                upv.archived,
                upv.primary_flag,
                upv.start_date,
                upv.end_date,
                upv.position,
                upv.position_id,
                upv.company_id
        from flow.user_positions_vw upv
        where upv.user_id = :userId
          and ((upv.end_date is null and upv.start_date <= now())
                 OR now() between upv.start_date and upv.end_date)
          and upv.archived is not true
        order by upv.start_date desc
  ]]></userPosition.getAllActive>

  <userPosition.getOne><![CDATA[
      select distinct upv.user_position_id as id,
                upv.user_id,
                upv.archived,
                upv.primary_flag,
                upv.start_date,
                upv.end_date,
                upv.position,
                upv.position_id,
                upv.company_id,
                uphv.hierarchy
      from flow.user_positions_vw upv
        inner join flow.user_position_hierarchy_vw uphv on uphv.user_id = upv.user_id and uphv.org_id = upv.org_id and uphv.position_id = upv.position_id
      where upv.user_position_id = :id
  ]]></userPosition.getOne>

  <userPosition.getUserPrimaryPosition><![CDATA[
    select distinct upv.user_position_id as id,
                    upv.user_id,
                    upv.archived,
                    upv.primary_flag,
                    upv.start_date,
                    upv.end_date,
                    upv.position,
                    upv.position_id,
                    upv.company_id
    from flow.user_positions_vw upv
    where upv.user_id = :userId
    and upv.archived is not true
    and upv.primary_flag is true
    and upv.archived is false
  ]]></userPosition.getUserPrimaryPosition>

  <userPosition.delete><![CDATA[
        update flow.user_position
        set archived = true,
            date_modified = now(),
            modified_by_id = :userId
    where id = :userPositionId
  ]]></userPosition.delete>

  <userPosition.updateUserPosition><![CDATA[
    update flow.user_position
        set position_id = :positionId,
            start_date = :startDate::date,
            end_date = :endDate::date,
            org_id = :orgId,
            date_modified = now(),
            modified_by_id = :modifiedById,
            primary_flag = :primaryFlag
    where id = :id
  ]]></userPosition.updateUserPosition>

  <userPosition.insertUserPosition><![CDATA[
    insert into flow.user_position(user_id, position_id, start_date, end_date, org_id, created_by_id, primary_flag)
    values (:userId, :positionId, :startDate::date, :endDate::date, :orgId, :createdById, :primaryFlag)
  ]]></userPosition.insertUserPosition>

  <userPosition.resetPrimaryFlags><![CDATA[
    update flow.user_position
        set primary_flag = false
    where id in (select up.id
                 from flow.user_position up
                          inner join flow.position p on p.id = up.position_id
                 where up.user_id = :userId
                   and p.company_id = :companyId
                   and up.id != :id
    )
  ]]></userPosition.resetPrimaryFlags>

</sql>

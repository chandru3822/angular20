<sql>
  <org.getAllForCompany><![CDATA[
    select o.*,
           p.org_name as parentOrgName,
           ot.org_type
    from flow.org o
        left join flow.org p on p.id = o.parent_org_id
        inner join flow.org_type ot on ot.id = o.org_type_id
    where o.company_id = :companyId
        and o.archived is not true
    order by o.org_name
  ]]></org.getAllForCompany>

  <org.getUsersInOrg><![CDATA[
    select u.id,
           u.first_name || ' ' || u.last_name as full_name,
           p.position,
           primary_flag
    from flow.user_position up
      inner join flow.user u on up.user_id = u.id
      inner join flow.position p on up.position_id = p.id
    where up.org_id = :orgId
      and up.archived is false
      and u.archived is false
      and up.primary_flag is true
      and up.start_date <= now() and (up.end_date is null or up.end_date >= now())
    order by full_name
  ]]></org.getUsersInOrg>

  <org.getSchedulingOrgs><![CDATA[
    select o.id,
     o.company_id,
     o.company_timezone_id,
     o.org_name,
     o.parent_org_id,
     o.org_type_id,
     o.active_flag,
     o.schedulable,
     o.available_to_children,
     o.company_state_id,
     cs.state_id,
     p.org_name as parentOrgName,
     o.org_name as title,
     ot.org_type
    from flow.org o
             left join flow.org p on p.id = o.parent_org_id
             inner join flow.org_type ot on ot.id = o.org_type_id
             left join flow.company_state cs on cs.id = o.company_state_id
    where case when :isParent and :isSchedulingTool
        then o.company_id = any (select id from flow.company_hierarchy_filter_down(:parentCompanyId::int))
        else (o.company_id = :companyId OR (:isSchedulingTool AND o.company_id = :parentCompanyId AND o.available_to_children is true )) end
      and case when :companyStateId::integer is not null
          then o.company_state_id = :companyStateId::integer and o.schedulable is true
          else o.schedulable is true end
      and o.archived is not true
      and o.active_flag is true
    order by o.org_name
  ]]></org.getSchedulingOrgs>

  <org.getOrgsByType><![CDATA[
    select o.*,
           p.org_name as parentOrgName,
           ot.org_type
    from flow.org o
        left join flow.org p on p.id = o.parent_org_id
        inner join flow.org_type ot on ot.id = o.org_type_id
    where o.company_id = :companyId
      and o.org_type_id = :typeId
      and o.active_flag is true
      and o.archived is not true
    order by o.org_name
  ]]></org.getOrgsByType>

  <org.searchOrgs><![CDATA[
    select o.*,
           p.org_name as parentOrgName,
           ot.org_type
    from flow.org o
        left join flow.org p on p.id = o.parent_org_id
        inner join flow.org_type ot on ot.id = o.org_type_id
    where o.company_id = :companyId
        and o.org_name ILIKE '%' || :query || '%'
        and o.archived is not true
    order by o.org_name
    limit :limit
    offset :offset ;
  ]]></org.searchOrgs>

  <org.searchOrgCount><![CDATA[
      select count(*)
      from flow.org o
          left join flow.org p on p.id = o.parent_org_id
          inner join flow.org_type ot on ot.id = o.org_type_id
      where o.company_id = :companyId
          and o.archived is not true
          and o.org_name ILIKE '%' || :query || '%'
  ]]></org.searchOrgCount>

  <org.exportOrgs><![CDATA[
    select
        o.org_name,
        ot.org_type,
        p.org_name as "parent_org_name",
        o.active_flag
    from flow.org o
        left join flow.org p on p.id = o.parent_org_id
        inner join flow.org_type ot on ot.id = o.org_type_id
    where o.company_id = :companyId
        and o.org_name ILIKE '%' || :query || '%'
        and o.archived is not true
    order by o.org_name desc;
  ]]></org.exportOrgs>

  <org.updateOrg><![CDATA[
    update flow.org
     set org_name = :orgName,
         parent_org_id = :parentOrgId,
         org_type_id = :orgTypeId,
         schedulable = :schedulable,
         available_to_children = :availableToChildren,
         company_timezone_id = :companyTimezoneId,
         company_state_id = :companyStateId,
         active_flag = :active,
         date_modified = now()
    where id = :id
  ]]></org.updateOrg>

  <org.insertOrg><![CDATA[
    insert into flow.org(company_id, org_name, parent_org_id, org_type_id, schedulable, company_state_id, available_to_children, company_timezone_id, date_created, date_modified)
    values(:companyId, :orgName, :parentOrgId, :orgTypeId, :schedulable, :companyStateId, :availableToChildren, :companyTimezoneId, now(), now())
  ]]></org.insertOrg>

  <org.getOne><![CDATA[
    select o.*,
           p.org_name as parent_org_name,
           ot.org_type,
           ot.org_parent_type_id as parent_org_type_id,
           s.abbreviation as state_abbreviation,
           s.state,
           t.timezone,
           coalesce((
                  SELECT array_to_json(array_agg(row_to_json(childOrgs)))
                  FROM (
                         SELECT childOrg.id,
                                childOrg.org_name as "orgName",
                                childOrg.active_flag as "activeFlag",
                                childOrgType.org_type as "orgType"
                         FROM flow.org childOrg
                           inner join flow.org_type childOrgType on childOrg.org_type_id = childOrgType.id
                         WHERE childOrg.parent_org_id = o.id
                           and childOrg.archived is not true
                         order by childOrg.active_flag desc, childOrg.org_name
                       ) childOrgs), '[]') AS "childOrgs"
    from flow.org o
        left join flow.company_state cs on o.company_state_id = cs.id
        left join flow.state s on cs.state_id = s.id
        left join flow.company_timezone ct on o.company_timezone_id = ct.id
        left join flow.timezone t on ct.timezone_id = t.id
        left join flow.org p on p.id = o.parent_org_id
        inner join flow.org_type ot on ot.id = o.org_type_id
    where o.id = :id
      and o.company_id = :companyId
  ]]></org.getOne>

  <org.getOrgFiltersForCompany><![CDATA[
    select of.id,
           of.org_level_id,
           ol.level,
           ol.level_name,
           of.rank,
           ol.company_id,
           of.show_type
    from flow.org_filter of
        inner join flow.org_level ol on ol.id = of.org_level_id
    where ol.company_id = :companyId
    order by of.rank
  ]]></org.getOrgFiltersForCompany>

  <org.getOneOrgFilter><![CDATA[
    select of.id,
           of.org_level_id,
           ol.level_name,
           of.rank,
           ol.company_id,
           of.show_type
    from flow.org_filter of
        inner join flow.org_level ol on ol.id = of.org_level_id
    where of.id = :id
  ]]></org.getOneOrgFilter>

  <org.updateOrgFilter><![CDATA[
      update flow.org_filter
        set org_level_id = :orgLevelId,
            rank = :rank,
            show_type = :showType
      where id = :id
  ]]></org.updateOrgFilter>

  <org.insertOrgFilter><![CDATA[
      insert into flow.org_filter(org_level_id, rank, show_type)
      values (:orgLevelId, :rank, :showType)
  ]]></org.insertOrgFilter>

  <org.deleteOrgFilter><![CDATA[
    delete from flow.org_filter
        where id = :id
  ]]></org.deleteOrgFilter>

  <org.getOrgsByHierarchyFilter><![CDATA[
    select * from flow.org_hierarchy_filter_down(ARRAY[ :selectedOrgs ]::integer[])
    union
    select * from flow.org_hierarchy_filter_up(ARRAY[ :selectedOrgs ]::integer[])
    order by org_level_id, org_name;
  ]]></org.getOrgsByHierarchyFilter>

  <org.getOrgsForLevel><![CDATA[
    select o.id,
           o.company_id,
           o.company_timezone_id,
           org_name,
           parent_org_id,
           org_type_id,
           active_flag,
           ot.org_type,
           ot.org_level_id,
           of.show_type
    from flow.org o
         inner join flow.org_type ot on ot.id = o.org_type_id
         inner join flow.org_level ol on ol.id = ot.org_level_id
         inner join flow.org_filter of on of.org_level_id = ol.id
    where o.company_id = :companyId
      and ot.org_level_id = :orgLevelId
      and o.archived is not true
      and o.active_flag is true
    order by o.org_name;
  ]]></org.getOrgsForLevel>

  <org.getOrgCalendarsForUser><![CDATA[
      select uoa.id,
             o.org_name,
             o.company_timezone_id,
             o.id as org_id,
             uoa.user_id,
             uoa.org_id,
             uoa.archived
      from flow.user_org_access uoa
               inner join flow.org o on o.id = uoa.org_id
      where uoa.user_id = :userId
          and uoa.archived is not true
          and o.archived is not true
          and (o.company_id = :companyId OR o.available_to_children is true)
      order by o.org_name;
  ]]></org.getOrgCalendarsForUser>

  <org.getOneOrgCalendarAccess><![CDATA[
      select uoa.id,
             o.org_name,
             o.company_timezone_id,
             uoa.user_id,
             uoa.org_id,
             uoa.archived
      from flow.user_org_access uoa
               inner join flow.org o on o.id = uoa.org_id
      where uoa.id = :id
  ]]></org.getOneOrgCalendarAccess>

  <org.saveOrgCalendarToUser><![CDATA[
      insert into flow.user_org_access(org_id, user_id, created_by_id, date_created, modified_by_id, date_modified)
      values (:orgId, :userId, :createdById, now(), :createdById, now())
  ]]></org.saveOrgCalendarToUser>


  <org.deleteOrgCalendarFromUser><![CDATA[
      update flow.user_org_access
        set archived = true,
            date_modified = now(),
            modified_by_id = :modifiedById
      where id = :id
  ]]></org.deleteOrgCalendarFromUser>

  <org.getOrgAttachments><![CDATA[
    select
      a.id,
      a.size,
      a.uuid,
      a.date_created,
      a.date_modified,
      a.filename,
      a.attachment_type_id,
      a.display_name,
      a.content_type,
      a.s3_key,
      a.archived,
      oa.linked,
      substring(filename, '\.([^\.]+)$') as file_extension,
      oa.org_id,
      concat(u.first_name, ' ', u.last_name) AS uploadedBy,
      att.attachment_type,
      orgn.*
    from flow.org_attachment oa
           inner join flow.attachment a on a.id = oa.attachment_id
           inner join flow."user" u ON a.created_by_id = u.id
           inner join flow.attachment_type att on a.attachment_type_id = att.id
           left join lateral ( select * from flow.get_attachment_origin(a.id)) orgn on true
    where oa.org_id = :orgId
      and case when :linked is true then oa.linked is true else oa.linked is false end
      and a.company_id = :companyId
      and oa.archived is false
      and a.archived is not true
    order by oa.date_created desc
  ]]></org.getOrgAttachments>

  <org.linkAttachment><![CDATA[
    insert into flow.org_attachment(attachment_id, org_id, created_by_id, linked)
    values(:attachmentId, :orgId, :userId, true)
  ]]></org.linkAttachment>

  <org.unlinkAttachment><![CDATA[
    update flow.org_attachment
      set archived = true,
          date_modified = now(),
          modified_by_id = :userId
    where attachment_id = :attachmentId
    and org_id = :orgId
    and linked is true
  ]]></org.unlinkAttachment>

  <org.addAttachment><![CDATA[
    insert into flow.org_attachment(attachment_id, org_id, created_by_id, date_created, modified_by_id, date_modified)
    values (:attachmentId, :orgId, :createdById, now(), :createdById, now())
  ]]></org.addAttachment>

</sql>

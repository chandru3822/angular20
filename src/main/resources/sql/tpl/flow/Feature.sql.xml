<sql>
  <feature.getAll><![CDATA[
    select f.id,
           f.feature_name,
           f.feature_code,
           f.archived,
           f.is_system
    from flow.feature f
    where f.archived is not true
    order by f.feature_name
  ]]></feature.getAll>

  <feature.getCompanyTools><![CDATA[
    select cf.id,
           cf.feature_name,
           f.feature_code,
           f.feature_path,
           f.is_system,
           cf.archived
    from flow.company_feature cf
        inner join flow.feature f on f.id = cf.feature_id
    where company_id = :companyId
    and cf.archived is not true
    and f.archived is not true
    and f.is_system is not true
    order by cf.feature_name
  ]]></feature.getCompanyTools>

  <feature.getAllForCompany><![CDATA[
    select cf.id,
           cf.feature_name,
           cf.company_id,
           cf.feature_id,
           cf.archived,
           f.feature_code,
           f.is_system
    from flow.company_feature cf
        inner join flow.feature f on f.id = cf.feature_id
    where cf.company_id = :companyId
    and cf.archived is not true
    order by cf.feature_name
  ]]></feature.getAllForCompany>

  <feature.getAllForCompanyWithAccess><![CDATA[
    select cf.id,
           cf.feature_name,
           cf.company_id,
           cf.feature_id,
           cf.archived,
           coalesce((
                        SELECT array_to_json(array_agg(row_to_json(accessControl)))
                        FROM (
                                 select ac.id,
                                        ac.access_level as "accessLevel",
                                        ac.access_code as "accessCode",
                                        false as enabled
                                 from flow.access_control ac
                                 where ac.archived is not true
                                 order by ac.display_order
                             ) accessControl), '[]') AS "accessControl"
    from flow.company_feature cf
    where cf.company_id = :companyId
      and cf.archived is not true
    order by cf.feature_name
  ]]></feature.getAllForCompanyWithAccess>

  <feature.getForUser><![CDATA[
    select cf.id,
       cf.feature_name as "featureName",
       cf.company_id as "companyId",
       cf.feature_id as "featureId",
       cf.archived,
       coalesce((
                    SELECT array_to_json(array_agg(row_to_json(accessControl)))
                    FROM (
                             select ufac.id,
                                    ufac.user_id as "userId",
                                    ufac.company_feature_id as "companyFeatureId",
                                    ac.id as "accessControlId",
                                    ac.access_level as "accessLevel",
                                    ac.access_code as "accessCode",
                                    ufac.enabled
                             from flow.access_control ac
                                      left join flow.user_feature_access_control ufac on ac.id = ufac.access_control_id
                               and ufac.company_feature_id = cf.id
                                and ufac.user_id = :userId
                             order by ac.display_order
                         ) accessControl), '[]') AS "accessControl"
      from flow.company_feature cf
      where cf.company_id = :companyId
        and cf.archived is not true
      order by cf.feature_name
  ]]></feature.getForUser>

  <feature.getAccessForUser><![CDATA[
    WITH t as (
     select feature_id, cf.feature_name, f.feature_code, ac.access_code, uac.enabled
         from flow.user_feature_access_control uac
                  inner join flow.company_feature cf on cf.id = uac.company_feature_id
                  inner join flow.company c on c.id = :companyId
                  inner join flow.feature f on f.id = cf.feature_id
                  inner join flow.access_control ac on ac.id = uac.access_control_id
         where user_id = :userId
           and enabled is true
           and cf.company_id = :companyId
     )
    SELECT *
    FROM T
    UNION
    select feature_id, cf.feature_name, f.feature_code, ac.access_code, pac.enabled
    from flow.position_feature_access_control pac
        inner join flow.company_feature cf on cf.id = pac.company_feature_id
        inner join flow.company c on c.id = :companyId
        inner join flow.user_position up on up.position_id = pac.position_id and up.end_date is null and up.user_id = :userId
        inner join flow.feature f on f.id = cf.feature_id
        inner join flow.access_control ac on ac.id = pac.access_control_id
    WHERE pac.enabled is true
      and cf.company_id = :companyId
      and NOT EXISTS (SELECT 1 FROM t);
  ]]></feature.getAccessForUser>

  <feature.upsertUserFeatureAccessControl><![CDATA[
    insert into flow.user_feature_access_control(company_feature_id, access_control_id, user_id, enabled)
    values (:companyFeatureId, :accessControlId, :userId, true)
    ON CONFLICT (user_id, company_feature_id, access_control_id)
      DO UPDATE SET enabled = :enabled
  ]]></feature.upsertUserFeatureAccessControl>

  <feature.getOneCompanyFeature><![CDATA[
    select cf.id,
           cf.feature_name,
           cf.company_id,
           cf.feature_id,
           cf.archived,
           f.is_system,
           f.feature_code
    from flow.company_feature cf
        inner join flow.feature f on f.id = cf.feature_id
    where cf.id = :id
  ]]></feature.getOneCompanyFeature>

  <feature.insertCompanyFeature><![CDATA[
    insert into flow.company_feature(feature_name, company_id, feature_id)
    values (:featureName, :companyId, :featureId)
  ]]></feature.insertCompanyFeature>

  <feature.updateCompanyFeature><![CDATA[
    update flow.company_feature
        set feature_name = :featureName
    where id = :id
  ]]></feature.updateCompanyFeature>

  <feature.deleteCompanyFeature><![CDATA[
    update flow.company_feature
        set archived = true
    where id = :id
  ]]></feature.deleteCompanyFeature>

  <feature.getOneFeature><![CDATA[
    select f.id,
           f.feature_name,
           f.feature_code,
           f.archived,
           f.is_system
    from flow.feature f
    where f.id = :id
  ]]></feature.getOneFeature>

  <feature.insertFeature><![CDATA[
    insert into flow.feature(feature_name, feature_code, is_system)
    values (:featureName, :featureCode, :isSystem)
  ]]></feature.insertFeature>

  <feature.updateFeature><![CDATA[
    update flow.feature
        set feature_name = :featureName,
            feature_code = :featureCode,
            is_system = :isSystem
    where id = :id
  ]]></feature.updateFeature>

  <feature.deleteFeature><![CDATA[
    update flow.feature
        set archived = true
    where id = :id
  ]]></feature.deleteFeature>
</sql>

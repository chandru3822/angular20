<sql>
  <status.getTypesForCompany><![CDATA[
    select *
    from flow.status_type
  ]]></status.getTypesForCompany>

   <status.getType>
    <![CDATA[
      select id,
             status_type
      from flow.status_type st
      where id = :typeId
      order by st.status_type;
    ]]>
  </status.getType>
  <status.deleteType>
    <![CDATA[
        update flow.status_type
        set archived = true
        where id = :id
    ]]>
  </status.deleteType>
  <status.updateType>
    <![CDATA[
      update flow.status_type
        set status_type = :statusType
      where id = :id
    ]]>
  </status.updateType>
  <status.insertType>
    <![CDATA[
      insert into flow.status_type(status_type)
        values (:statusType)
      returning id;
    ]]>
  </status.insertType>
</sql>
